#!/usr/bin/env python3


class LVNIdentifier:
    def __init__(self, identifier):
        self._name = None
        self._number = None
        if isinstance(identifier, str):
            self._name = i
        elif isinstance(_name)
            self._number = identifier
        else:
            print("Cannot handle this type of identifier")
            quit()


class LVNValue:
    def __init__(self, operator, operands):
        self.operator = operator
        self.operands = operands
